import type { CandidateMaxEvidence } from "./gen5-pve-types";

export const maxEvidence644649: Readonly<Record<string, CandidateMaxEvidence>> = {};

export function candidateMaxEvidence644649(variantId: string) {
  return maxEvidence644649[variantId] ?? null;
}
