/** The highest species included in the currently published data snapshot. */
export const CURRENT_DATA_MAX_DEX = 386;
export const CURRENT_DATA_SCOPE = `001-${String(CURRENT_DATA_MAX_DEX).padStart(3, "0")}`;
